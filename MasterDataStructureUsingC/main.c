#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Node
{
    int data;
    struct Node *next;
} Node;

int main()
{/* 
    Node *P = NULL;
    Node *Q;
    P = Q;

    *P = (Node *)malloc(sizeof(Node));

    Q = P->next;

    P = P->next;
 */


    Node *P = NULL;

    if (P == NULL)
    {
        
    }

    return 0;
}